# ----------------------------------------------------------------------------------------
# alias file
# ----------------------------------------------------------------------------------------

# Quick edit
alias zshconfig='nvim ~/.zshrc; source ~/.zshrc' 
alias malias='nvim ~/.aliases.zsh; source ~/.aliases.zsh'
alias todo="nvim ~/.todo.txt"

# rebind vim to nvim 
alias vim="nvim"
alias vi="nvim"

# Qucik acsess and movement
alias ohmyzsh="cd ~/.oh-my-zsh"
alias e="nautilus --browser $args"
alias cd..="cd .."
alias dotfiles="git -C ~/.dotfiles $args"


# vpn to ntnu
alias nvpn="sudo openconnect vpn.ntnu.no"
# activate python venv
alias activate="source venv/bin/activate"

# always open tmux in main window
alias tm='tmux new -A -s main'
# p10k color scheme
alias p10k_color='for i in {0..255}; do print -Pn "%K{$i}  %k%F{$i}${(l:3::0:)i}%f " ${${(M)$((i%6)):#3}:+$'\n'}; done'

source ~/.aliases_local

alias virtualenv="python3 -m virtualenv $args"

alias la='ls -a'

